# PureLab UI 设计方案 —— 美观与易用的平衡（v2.11 提案）

> 基线：v2.10.0（横屏 + 排版治理已落地）。本方案在**不动信息架构、不推翻既有令牌**的前提下，把视觉质感升级到「液态玻璃」，并系统化易用性规则。
> 所有色值均标注实测对比度（对 `--bg #F9F2EA` / 对白卡），保证升级不回退无障碍成果。

---

## 一、设计目标

| 维度 | 现状（v2.10） | 目标（v2.11） |
|---|---|---|
| 美观 | 米白暖调纸面感，扁平卡片 | 液态玻璃层次：半透明面板 + 背景模糊 + 微渐变，呼应 iOS 26 质感 |
| 易用 | 44px 触达全覆盖、文字排版归零 | 拇指动线分区（热区/可达/危险三带）、状态可预期、动效反馈闭环 |
| 一致 | 令牌分散在 css 各段注释里 | 令牌收拢为 Design Token 一级段 + 玻璃层新令牌 |
| 无障碍 | AA 全达标 | 维持 AA，禁止用玻璃质感牺牲可读性 |

**三条铁律**（继承自 v38/v40 教训）：
1. **对比度优先于美观**——玻璃层上必须垫白/米白实底或提高模糊不透明度，文字颜色不变；
2. **只动画 `transform` 与 `opacity`**——`backdrop-filter` 不参与动画，避免 Android WebView 掉帧；
3. **像素以实测为准**——任何视觉改动后跑 `verify38/verify_nb/verify_land/scan_typo` 四件套回归。

---

## 二、用户习惯画像（基于 24 张截图 + 19 个交互状态实测）

- **单手竖持为主**：屏宽集中在 360–412px，`#app` 已限宽 460px；拇指自然弧线覆盖屏幕下半 2/3。
- **高频操作**：孔板选孔/分配确认（s08/s09）、数值录入（s10）——都发生在屏幕中下部，按钮应「就拇指」而非「就视线」。
- **低频危险操作**：删除实验/组合/归档——已统一走确认弹层 + Toast 撤销，保持不动。
- **阅读顺序**：中文标题 → 英文小字 → 数据数字。`font-feature-settings:"tnum"` 已全局生效，表格数字对齐是习惯资产，玻璃化时保留。
- **增量迭代偏好**：新视觉必须叠加在旧布局上，不整页重排；用户靠位置记忆操作。

---

## 三、设计令牌（Design Tokens）

### 3.1 色彩表（保留 7 色基准 + 安全派生，新增玻璃层）

| 令牌 | 色值 | 用途 | 对比度 |
|---|---|---|---|
| `--bg` | `#F9F2EA` | 页面底色 | — |
| `--ink` | `#2F3A31` | 一级文字/主强调 | 11.4:1 |
| `--ink2` | `#536253` | 二级文字 | 6.1:1 |
| `--ink3t` | `#666E61` | 三级文字（文本专用） | 4.77:1 / 5.29:1 |
| `--warm-ink` | `#7E5F36` | 暖棕数字/强调文字 | 5.29:1 / 5.87:1 |
| `--danger` | `#9A5147` | 删除/错误 | 5.19:1 / 5.77:1 |
| `--warm` | `#C0A381` | 仅图形/描边/深绿底文字 | 禁止作浅底文本 |
| **新增** `--glass` | `rgba(255,255,255,.55)` | 玻璃面板基底 | — |
| **新增** `--glass-strong` | `rgba(255,255,255,.78)` | 玻璃面板·强（承载正文） | — |
| **新增** `--glass-line` | `rgba(255,255,255,.65)` | 玻璃面板 1px 高光描边 | — |
| **新增** `--blur` | `backdrop-filter:blur(14px) saturate(1.4)` | 玻璃模糊（配 `-webkit-` 前缀） | — |

深绿主卡（`.exp-card.dark` / `.hero.dark`）维持 `linear-gradient(135deg,#536253,#2F3A31)`，是全应用的视觉锚点，不做玻璃化——**大面积模糊留给浅色面板，深色卡靠渐变本身的高级感**。

### 3.2 字体刻度（8px 基线，已有体系收拢）

| 级别 | 字号/行高 | 用途 |
|---|---|---|
| Display | 34px/1.2 | s11 hero 回收率大数字 |
| Title | 17px/600 | 导航标题、卡标题 |
| Body | 14px/1.5 | 正文（学术规范同款） |
| Caption | 12px/1.4 | 辅助说明、meta |
| Micro | 10–11px | 英文字母间距标（`.en` 0.22em）、角标 |

### 3.3 间距 / 圆角 / 阴影

- 间距单位：4px 基数，页面左右 20px、卡内 14–16px、卡间 10–14px（维持现状，不重排）。
- 圆角：卡 14px（`--r-card`）、输入 10px、玻璃面板 16px（玻璃要更「圆润漂浮」）。
- 阴影两档：
  - `--shadow`（现有）：`0 1px 3px rgba(47,58,49,.08)` —— 常态卡；
  - `--shadow-float`（新增）：`0 8px 24px rgba(47,58,49,.14)` —— 玻璃面板/悬浮元素。

---

## 四、液态玻璃视觉体系

### 4.1 应用范围（克制原则）

| 元素 | 处理 | 理由 |
|---|---|---|
| 顶部导航 `.nav` | ✅ 玻璃（吸顶，滚动时内容从下面穿过最能体现质感） | 高频注视区 |
| 底部固定操作条/主按钮 | ✅ 玻璃强档 `--glass-strong` | 拇指热区，需最高可读性 |
| 弹层/确认框/Toast | ✅ 玻璃强档 | 悬浮层天然适合 |
| 普通内容卡 | ❌ 保持纸面白卡 | 大面积模糊成本高且显得「糊」 |
| 孔板 `.plate` | ❌ 不玻璃化 | 数据密度区，清晰度第一 |
| 深绿主卡 | ❌ 保持渐变 | 视觉锚点 |

### 4.2 标准写法（含降级）

```css
.glass{
  background:var(--glass);
  -webkit-backdrop-filter:var(--blur);
  backdrop-filter:var(--blur);
  border:1px solid var(--glass-line);   /* 高光描边是玻璃质感的关键一笔 */
  box-shadow:var(--shadow-float);
  border-radius:16px;
}
/* Android WebView < 76 或低端机降级：不透明面板，观感仍完整 */
@supports not (backdrop-filter: blur(1px)) {
  .glass{ background:var(--paper); border-color:var(--line); box-shadow:var(--shadow); }
}
```

### 4.3 背景氛围光（可选，Phase 2）

在 `body` 上加两团极淡的固定色斑（`radial-gradient`，`transform` 定位、不动画），玻璃模糊才有东西可「透」：

```css
body::before{content:"";position:fixed;inset:0;z-index:-1;
  background:
    radial-gradient(320px 320px at 85% -5%, rgba(192,163,129,.18), transparent 70%),
    radial-gradient(400px 400px at -10% 90%, rgba(83,98,83,.10), transparent 70%);}
```

### 4.4 性能守则

- `backdrop-filter` 只挂在**吸顶/悬浮层**等少量节点（每屏 ≤3 个），不进列表循环渲染；
- 动效只写 `transform/opacity`，时长 0.2–0.3s、`ease`；
- 玻璃节点加 `will-change:transform` 会强制分层，仅在确有动画时使用。

---

## 五、布局与易用性规则

### 5.1 拇指三分区（360–412px 竖屏）

| 分区 | 区域 | 规则 |
|---|---|---|
| 安全区 | 顶部 25%（视觉为主） | 导航、标题、状态标签；只放小按钮（≥34px） |
| 可达区 | 中部 45% | 内容卡、孔板、输入表单 |
| 热区 | 底部 30% | **主操作按钮、固定操作条**；目标 ≥48px，间距 ≥12px |

现状检查：s05「开始分配」、s09「完成」类主按钮已近底部 ✔；若后续新增固定操作条，直接套 `.glass` + 热区规则。

### 5.2 断点（移动优先）

| 宽度 | 行为 |
|---|---|
| ≤340px | 已有降级档（`.rank-assay` 隐藏、间距收紧），保持 |
| 341–412px | 主档，一切默认样式按此设计 |
| ≥768px（平板） | `#app` 居中限宽 460px + 两侧留白透出氛围光；`.nb` 括号回一行（v2.9 已验证） |

### 5.3 状态可预期（组件状态矩阵）

每个可交互组件四态齐备，样式差异**不仅靠颜色**（WCAG：不得仅以颜色传达状态）：

| 组件 | 常态 | 悬停/聚焦 | 按下 | 选中/成功 | 禁用 |
|---|---|---|---|---|---|
| 主按钮 | 深绿渐变+`--shadow` | — | scale(.95) | — | opacity .4 + 无阴影 |
| 卡片选择 | 白卡 | — | border 亮 1.5px | **✓ 圆标 + 深绿描边**（现 `combo-check` 模式） | — |
| 输入框 | 白底 `--line` 描边 | — | 描边转 `--ink` 2px | — | opacity .5 |
| Tab/步骤 | `--ink3t` | — | — | `--ink` + 加粗 + 实心圆 | — |

---

## 六、微交互参数

| 交互 | 参数 | 现状 |
|---|---|---|
| 按钮按下 | `scale(.95)`，0.15s | 部分已有（`.nav-avatar`），推广到主按钮 |
| 屏切换 | `fadein .18s`（仅 opacity） | 已有，保留 |
| Toast | 出场 translateY(8px)→0 + opacity，0.2s；4.2s 自动收，带撤销 | 已有，出场动效可补 |
| 轮播 | 惯性 + 居中磁吸，`will-change:transform,opacity` | 已有 |
| 数字变化 | 回收率等关键数字：不闪色、仅 `b` 加粗渐入 0.2s | 新增建议 |
| 玻璃面板 | **模糊与透明度永不参与动画** | 铁律 |

---

## 七、无障碍验收清单（升级后必查）

- [ ] 玻璃面板上所有文本 ≥4.5:1（重点：`--ink3t`/`--warm-ink` 在 `--glass-strong` 上复测，模糊层按最透情形算）；
- [ ] 44px 触达零回退（跑预览壳触达框覆盖层，预期「不足 44px = 0 个」）；
- [ ] `@supports not (backdrop-filter…)` 降级态同样过对比度扫描；
- [ ] 状态不依赖单一颜色（选中必有 ✓/描边/加粗之一）；
- [ ] 动效可关：`@media (prefers-reduced-motion: reduce){ *{animation:none!important;transition:none!important} }`（新增）。

---

## 八、分阶段实施计划

| 阶段 | 内容 | 改动面 | 验收 |
|---|---|---|---|
| **P1 玻璃骨架** | 新令牌入 css 头部；`.nav` / 弹层 / Toast 玻璃化；氛围光背景 | app.css（+~40 行）、无 js | 四件套回归全绿 + 新增 `verify_glass.js`（玻璃节点数 ≤3/屏、降级态对比度、触达 0 不足） |
| **P2 微交互补齐** | 主按钮按压缩放、Toast 出场、reduced-motion 块 | app.css 为主 | 截图驱动：360/390 双档各 3 个交互态复核 |
| **P3 细节打磨** | 输入框聚焦描边、空态插画式留白、深绿卡高光描边微调 | app.css | 用户截图确认，不引入横向滚动 |

每阶段独立出预览（`build_preview.py` 重跑），用户过目侧边栏预览后再进下一阶段；APK 打包在 P3 确认后统一执行（v2.11.0 · versionCode 43）。

---

## 九、风险与取舍

1. **backdrop-filter 在部分国产 WebView 的开销**——限定悬浮层使用 + `@supports` 降级，最坏情况回退到 v2.10 观感；
2. **玻璃质感与「实验工具」的专业感冲突**——通过「数据区（孔板/表格/数字）永不玻璃化」的分区原则化解，装饰止步于导航与悬浮层；
3. **暖棕 `--warm` 严禁因玻璃层变「便宜」**——它只出现在描边与深绿底上，玻璃高光描边用白色系 `--glass-line`。
