

######## 第一阶段提示词.docx ########
你现在不要写任何代码。

我正在开发一个“高通量重结晶实验 App”，目标平台是移动端手机。

项目中已经提供了 14 张经过确认的 UI 设计参考图，它们是本项目的视觉设计依据。请先完整查看项目中的所有参考图片，不要只看其中几张。

参考图片目录：

/design-reference/

其中包含：

01_welcome.png
02_workspace.png
03_new_experiment.png
04_plate_configuration.png
05_run_experiment.png
06_results.png
07_experiment_record.png
08_compare.png
09_experiment_design.png
10_templates.png
11_export_record.png
12_next_experiment.png
13_well_detail.png
14_experiment_settings.png

同时阅读：

DESIGN_SYSTEM.md
PAGE_SPEC.md
UX_FLOW.md
DATA_MODEL.md

现在你的任务只有一个：

分析整个产品，不要实现。

请输出一份《项目 UI 与产品结构分析报告》，必须包含以下内容：

1. 产品定位
说明这个 App 是什么，以及明确说明它不是什么。

2. 整体视觉系统
分析：
- 页面背景
- 主文字颜色
- 辅助文字颜色
- 主强调色
- 状态色
- 线条
- 阴影
- 圆角
- 字体层级
- 间距
- 留白
- 页面密度
- 中英文排版方式

3. 全局 UI 组件
识别 14 张图中反复出现的公共组件，例如：
- 顶部导航
- 页面标题
- 区块标题
- 状态标识
- 实验信息
- 条件列表
- 时间线
- 图片记录
- 操作入口
- Bottom Sheet
- 轻量弹窗
- 文本 CTA
等。

4. 96 孔板系统
这是本项目最重要的公共组件之一。
请详细分析：
- 8×12 结构
- A-H 行
- 1-12 列
- 孔位尺寸
- 孔间距
- 行列标签
- 已配置状态
- 进行中状态
- 已完成状态
- 异常状态
- 选中状态
- 空孔状态
- 结果状态
- 缩略图状态
- 不同页面中的复用关系

5. 数据模型
分析：
Experiment
Variable
Level
Condition
Well
Observation
Result
Selected Condition
Experiment Record
Template
Experiment Lineage
这些对象之间是什么关系。

6. 页面关系
分析 14 个页面之间的导航关系和数据流关系。

7. 页面逐页结构
对 14 个页面分别说明：
- 页面目的
- 用户进入条件
- 核心内容
- 核心交互
- 页面最重要的视觉对象
- 数据来源
- 可以跳转到哪些页面

8. 视觉一致性风险
找出最容易在后续开发中被做错的地方。

9. 实现建议
在不写代码的前提下，提出应该如何进行组件化、页面拆分和数据管理。

10. 开发原则
明确列出哪些视觉和产品行为绝对不能被后续开发擅自改变。

重要要求：

第一：
14 张参考图不是“灵感”，而是正式设计依据。

第二：
不要重新设计 UI。

第三：
不要自行增加新的视觉风格。

第四：
不要把它做成普通 SaaS Dashboard。

第五：
不要增加 AI、智能推荐、最佳条件、自动优化等功能。

第六：
不要把 96 孔板做成普通 Excel 表格。

第七：
不要为不同页面分别创建不同的 96 孔板样式。

第八：
整个项目中的 96 孔板必须是统一、可复用的核心组件。

第九：
不要写代码。

第十：
分析完成后停止，等待我下一步指令。


######## 第二阶段提示词.docx ########
现在进入第二阶段。

这一阶段仍然不要实现完整业务页面。

你的任务是：

根据刚才的分析结果，以及项目中的 14 张参考图，建立整个项目的统一 Design System 和可复用组件体系。

目标不是快速写页面，而是先建立一套稳定的视觉基础和公共组件。

请先完成以下工作：

一、建立全局 Design Token

至少定义：

1. 页面背景色
2. 一级文字颜色
3. 二级文字颜色
4. 三级文字颜色
5. 主强调色
6. 选中状态色
7. 成功状态色
8. 异常状态色
9. 分隔线颜色
10. 输入区域颜色
11. 卡片/面板背景
12. 阴影
13. 圆角
14. 页面边距
15. 区块间距
16. 行间距
17. 字体大小
18. 字体重量
19. 英文标题样式
20. 数字样式

所有页面必须使用这些统一 Token。

不要让每个页面自己定义颜色、字号和间距。

二、建立全局 UI 组件

请建立可复用组件，并说明每个组件用途。

至少包括：

AppShell
TopNavigation
PageHeader
SectionHeader
ExperimentHeader
StatusIndicator
Divider
TextAction
ActionRow
ExperimentMeta
ConditionRow
ConditionList
ConditionDetail
Timeline
VisualRecord
ObservationSelector
ResultField
BottomSheet
ConfirmDialog
EmptyState

三、重点建立统一的 96 孔板组件系统

这是整个项目最重要的组件。

必须设计：

WellPlate
Well
WellCoordinate
WellState
WellLegend
PlateMiniMap

要求：

1. 永远保持 8×12。
2. 永远使用 A-H。
3. 永远使用 1-12。
4. 不同页面不能各写一套孔板。
5. 孔位尺寸和间距必须统一。
6. 通过状态参数控制显示，而不是复制组件。
7. 支持配置状态。
8. 支持执行状态。
9. 支持结果状态。
10. 支持选中状态。
11. 支持异常状态。
12. 支持空状态。
13. 支持缩略图模式。
14. 支持完整大板模式。
15. 支持不同数据指标映射。

例如同一个 WellPlate 应该能够支持：

配置模式
执行模式
结果模式
档案模式
下一轮实验预览
单孔详情中的位置定位

四、建立统一的 Condition 数据结构

不要让各页面自己保存条件。

统一管理：

Experiment
Variable
Level
Condition
Well
Observation
Result
Template
SelectedCondition

五、建立统一导航模型

确保以下页面可以共享公共导航结构：

欢迎页
实验工作台
实验设计
条件确认
96 孔板配置
实验执行
实验结果
实验档案
条件比较
实验模板
导出实验记录
下一轮实验
单孔详细记录
实验设置

六、严格控制视觉自由度

后续实现过程中：

禁止重新发明组件。

禁止自行增加按钮样式。

禁止自行增加渐变。

禁止增加霓虹效果。

禁止增加 AI 视觉。

禁止增加生物学元素。

禁止增加大量卡片。

禁止加入 Bottom Tab。

禁止把 CTA 改成大圆角按钮。

禁止改变 96 孔板视觉系统。

七、这一阶段暂时不要完整实现 14 个页面。

先完成：

Design Tokens
Shared Components
Well Plate Component
Core Data Model
Navigation Structure

完成后列出你建立的组件和结构。

不要开始批量生成 14 个页面。

完成后停止，等待下一步指令。


######## 第三阶段提示词.docx ########
现在进入第三阶段：开始实现页面。

这一阶段先只实现前 3 个页面：

01_welcome.png
02_workspace.png
03_new_experiment.png

严格使用：

/design-reference/

中的对应参考图。

同时必须遵循：

/docs/DESIGN_SYSTEM.md
/docs/PAGE_SPEC.md
/docs/UX_FLOW.md
/docs/DATA_MODEL.md

实现原则：

1. 参考图片是最终视觉依据。
2. 不要自由发挥。
3. 不要重新设计布局。
4. 不要改变信息层级。
5. 不要改变主要元素的位置关系。
6. 不要改变页面整体密度。
7. 不要增加参考图中不存在的功能。
8. 不要删除主要元素。
9. 不要自行使用新的颜色。
10. 不要自己发明新的按钮样式。

重点还原：

- 页面比例
- 顶部安全区
- 顶部导航
- 页面标题
- 英文副标题
- 实验名称
- 区块位置
- 文字层级
- 字体重量
- 行距
- 分隔线
- 留白
- 卡片/面板比例
- CTA
- 图标
- 交互区域

所有页面必须使用之前已经建立的公共组件。

特别注意：

不能为这三个页面单独写新的视觉系统。

不能单独写新的 WellPlate 组件。

不能出现三个不同版本的页面标题。

完成后先不要继续做第 4 页以后。

先检查这 3 个页面：

1. 页面是否与参考图属于同一套设计系统。
2. 是否存在布局偏移。
3. 是否存在颜色偏移。
4. 是否存在字号偏差。
5. 是否存在间距问题。
6. 是否存在不必要的 UI 元素。
7. 是否存在缺少的 UI 元素。

完成视觉校正后，停止。


######## 第四阶段提示词.docx ########
现在继续实现：

04_plate_configuration.png
05_run_experiment.png
06_results.png

严格按照对应参考图实现。

特别检查：

1. 三个页面必须使用同一个 WellPlate 组件。
2. 96 孔板必须始终是 8×12。
3. A-H 和 1-12 必须准确。
4. 孔位大小和间距必须与前面页面一致。
5. 配置、执行、结果三个状态必须通过数据状态变化实现，而不是创建三套孔板。
6. C7 必须保持为 C7。
7. 页面视觉密度必须与前 3 页统一。
8. 不得加入 AI 推荐。
9. 不得加入最佳条件。
10. 不得加入新的视觉风格。

完成后进行视觉检查并停止。


######## 第五阶段提示词.docx ########
现在继续实现：

07_experiment_record.png
08_compare.png
09_experiment_design.png

继续使用现有 Design System 和公共组件。

不要重新定义视觉系统。

特别检查：

- 实验档案必须具有文档化、可追溯的视觉感。
- 条件比较必须突出横向数据比较。
- 实验设计必须突出变量、水平和条件数量。
- 三个页面必须保持相同的字体、颜色、线条和间距系统。
- 不要引入新的 SaaS Dashboard 风格。
- 不要引入 AI 功能。

完成后进行视觉检查并停止。


######## 第六阶段提示词.docx ########
现在继续实现：

10_templates.png
11_export_record.png
12_next_experiment.png
13_well_detail.png
14_experiment_settings.png

严格按照对应参考图实现。

保持：

- 同一套 Design System
- 同一套导航
- 同一套字体
- 同一套颜色
- 同一套间距
- 同一套状态体系
- 同一套 WellPlate 组件
- 同一套 Condition 数据结构

重点：

模板只保存实验设计结构，不保存实验结果。

导出页面只负责选择导出内容和格式。

下一轮实验只允许基于用户主动选择的条件建立新设计，不允许自动推荐。

Well Detail 自动读取 Condition 和 Well 数据，不要求用户重新输入实验条件。

Experiment Settings 只管理当前实验档案，不要做成系统设置页面。

完成后对全部 14 个页面做一次全局一致性检查。


######## 第七阶段提示词.docx ########
现在不要新增功能。

请对整个项目进行一次“视觉还原与一致性验收”。

使用项目中的 14 张参考图作为唯一视觉标准。

逐页检查：

01_welcome
02_workspace
03_new_experiment
04_plate_configuration
05_run_experiment
06_results
07_experiment_record
08_compare
09_experiment_design
10_templates
11_export_record
12_next_experiment
13_well_detail
14_experiment_settings

重点检查以下内容：

一、整体视觉

- 背景是否统一
- 文字颜色是否统一
- 主强调色是否统一
- 状态颜色是否统一
- 字体是否统一
- 英文标题是否统一
- 数字字体是否统一
- 圆角是否统一
- 阴影是否统一
- 分隔线是否统一
- 页面左右边距是否统一
- 区块间距是否统一

二、导航

检查所有页面：

- 返回位置
- 页面标题
- 英文副标题
- 右侧菜单
- 安全区

是否统一。

三、96 孔板

这是最高优先级。

检查：

- 必须是 8×12
- A-H 正确
- 1-12 正确
- 孔位大小一致
- 孔距一致
- 标签位置一致
- 选中状态一致
- 异常状态一致
- 已完成状态一致
- 空状态一致
- 缩略图和完整视图保持同一视觉语言

四、信息密度

不能让某一页突然变成：

- Dashboard
- Excel
- 巨型卡片墙
- 大型表单

五、CTA

检查所有主要操作：

必须遵循统一文字 CTA + 细线体系。

禁止：

- 巨型按钮
- 胶囊按钮
- 箭头按钮
- Bottom Tab

六、科学逻辑

检查整个项目：

绝对不能出现：

- AI 推荐
- 最佳条件
- 自动优化
- 最优溶剂
- 最优温度
- 自动化学结论

七、数据一致性

确保：

C7 永远是 C7。

B5 永远是 B5。

D8 永远是 D8。

Experiment ID 始终保持一致。

Condition 数量保持一致。

同一个实验在不同页面的数据必须相互对应。

八、不要修改设计

如果只是视觉偏差，直接修正。

如果需要新增功能或者改变产品逻辑，不要擅自修改，先报告问题。

最后输出：

《14 页面视觉一致性检查报告》

包括：

通过项
问题项
严重问题
需要修正的页面
建议修正顺序

完成检查后再进行必要的视觉修正。
