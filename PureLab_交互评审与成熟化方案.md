# PureLab V1 交互评审与成熟化方案

> 评审日期：2026-09-13
> 评审对象：`purelab-apk/app/assets/www/`（index.html · app.css v36 · app.js v37）+ `MainActivity.java`
> 方法：源码全量通读 + Playwright 驱动 Chrome 实机渲染（390×844@2x 与 320×640 双档，19 个交互状态）+ WCAG 2.2 对比度/触达尺寸自动度量
> 取证：24 张截图、`audit.json` 度量原始数据；**全程 0 个 JS 运行时错误，20 个状态横向无溢出**（`scrollWidth == innerWidth`）
> 落实：**已完成前两批改造，见第八节**（v2.8.0 · app.css/app.js v38）

---

## 一、结论

**功能闭环已经完全成立，卡住它成为"成熟产品"的不是功能，而是三件事：关键信息看不清、危险操作收不回、状态边界没交代。**

| 维度 | 现状 | 判定 |
|---|---|---|
| 信息可读性（WCAG 2.2 AA） | 实测 35+ 处样式类不达 4.5:1，**最低 2.36:1** | ❌ 未达标 |
| 触达可达性 | 导航返回 34×34、组合卡"详情/删除" 22×17、chip 35×28，共 14 类低于 44×44 | ❌ 未达标 |
| 操作可预期（反馈/撤销） | 有确认弹层，**无任何撤销通道** | ⚠️ 不足 |
| 状态完整性（空/错/满） | s12/s16 有空态，**s11 结果页、s05 分配页缺空态** | ⚠️ 不足 |
| 数据可恢复 | 只能导出 JSON 文本，**无法导入回填** | ❌ 硬伤 |
| 效率（任务步数） | 单孔录入最多 3 跳；批量不支持跨行零散孔 | ⚠️ 可优化 |
| 视觉完成度 | 令牌体系自洽、动效克制、深绿主卡层级清晰 | ✅ 良好 |

---

## 二、P0 · 影响"看得清 / 不会做错"，建议下一版必修

### P0-1 全 App 最重要的数字，恰好是对比度最差的那个

实测数据（WCAG 相对亮度算法，取各屏最差实例）：

| 用途 | 颜色 | 背景 | 实测对比度 | AA 要求 | 影响面 |
|---|---|---|---|---|---|
| 最佳回收率数字（s07 19px / s12 26px / s15 19px / s13 名次） | `--warm #C0A381` | 白卡 `#FFFFFF` | **2.39:1** | 3.0:1（大字）/ 4.5:1 | 6 处 |
| 组合卡「删除」文字 | `--warm` | 白卡 | 2.39:1 | 4.5:1 | 13 处 |
| 说明文字 `.hint`、`.combo-recipe`、`.k`、`.unit`、`.tl-t`、`.rg-cas`、`.dist-k` | `--ink3 #9CA296` | 米白 `#F9F2EA` | **2.36:1** | 4.5:1 | 约 30 处 |
| 同上（落在白卡上） | `--ink3` | `#FFFFFF` | 2.62:1 | 4.5:1 | 约 20 处 |
| 导航英文副标 `.en` / `.sec-en` | `--ink3` | 米白 | 2.36:1 | 4.5:1 | 35 处 |

有意思的是：同一套暖棕在**深绿主卡**上是 **4.97:1**，完全合格。也就是说，品牌色本身没问题，是"暖棕用在米白/白底"这个组合出了错。

**改法**：不动既有 7 色令牌，只新增两个"文本安全级"派生色，并把文本用色批量替换。

```css
:root{
  /* 新增：文本安全级（原色保留给图形/分隔/描边使用） */
  --ink3t:#666E61;   /* 4.77:1 on #F9F2EA · 5.29:1 on #FFF —— 替代 ink3 的所有文本用途 */
  --warm-ink:#7E5F36;/* 5.29:1 on #F9F2EA · 5.87:1 on #FFF —— 白底上的暖棕数字/文字 */
}
```

- `.hint .combo-recipe .k .unit .tl-t .rg-cas .dist-k .en .sec-en .arch-sub .exp-recipe .assay-sub .wnav-k .rg-abbr .fsuffix .rank-no .step` → `color:var(--ink3t)`
- `.stat .v.warm, .kv .v.warm, .rank-no.top, .hero .big`（白底场景）→ `color:var(--warm-ink)`
- `.hero.dark` 内的暖棕**保持不变**（4.97:1 合格，且深绿卡上暖棕是设计亮点）
- `.well.empty` 的虚线描边继续用 `--ink3`（图形元素仅需 3:1）

> 验收：重跑度量脚本，`lowContrast` 数组应为空。

### P0-2 温度和浓度在孔板上"看不见"

孔板颜色只编码了**组合**（5 色）。当条件是"组合 × 温度 × 浓度"时，`自动分配` 会把 2 个温度铺成上下两行同色 —— 用户盯着板子分不清哪行是 60℃、哪行是 70℃，只能退回图例逐行数。

s05 实测：`3 种组合 × 2 温度 = 6 行`，孔板上 A 行与 B 行、C 行与 D 行、E 行与 F 行**颜色完全相同**。

**改法（推荐 A，成本最低见效最快）**

- **A. 孔板右侧加"条件条"**：grid 从 `20px repeat(12,1fr)` 扩为 `20px repeat(12,1fr) 46px`，末列每行显示 `A·60℃·10` 的竖排缩写，底色 = 该行组合色 12% 淡色。
  ```css
  .plate{grid-template-columns:20px repeat(12,1fr) 46px}
  .pcond{font-size:9px;color:var(--ink3t);letter-spacing:0;line-height:1.15;
         text-align:left;padding-left:3px;font-weight:600}
  ```
  ⚠️ 320px 屏需降级：`@media (max-width:360px){ .plate{grid-template-columns:16px repeat(12,1fr)} .pcond{display:none} }`
- **B. 孔内同心环表示温度档位**：第 1 档实心，第 2 档单内环，第 3 档双内环（`::after` + `box-shadow: inset`）。
  优点：不占横向空间，320px 也能用；缺点：21px 的孔里画两圈环，分辨率已接近极限，实测会糊。
- **C. 图例补行号映射**：`legend-config` 每项前置 `A 行 ·`，成本几乎为零，可立即做。

**建议 A + C 同时做，B 不做。**

### P0-3 危险操作没有退路，而且和浏览入口平级

- s11 底部三个同级文字链接：`最佳条件 / 结果排名 / 完成并归档`。"完成并归档"会**把实验从进行中列表移走**，视觉权重却和两个"只是看看"的入口完全一样。
- 删除实验、删除归档、删除组合、重置数据：全部是"确认弹层 → 立即生效 → 不可恢复"。删错了只能重录 96 孔。
- 「删除组合」尤其危险：已分配该组合的孔位会退化成兜底灰色（`comboOf` 兜底分支），数据还在但视觉语义丢失。

**改法**

1. **统一撤销通道**：所有删除成功后的 toast 改为可撤销 toast（3.5 秒内可点「撤销」）。
   ```js
   function toastUndo(msg, onUndo){
     var t=$('toast');
     t.innerHTML = esc(msg) + '<button id="toast-undo">撤销</button>';
     t.classList.add('show','undo');
     clearTimeout(toastTimer);
     toastTimer = setTimeout(function(){ t.classList.remove('show','undo'); }, 3500);
     $('toast-undo').onclick = function(){ t.classList.remove('show','undo'); onUndo(); };
   }
   ```
   实现上把 `leaveThenDeleteExp/Arch` 的删除动作从"动画结束即执行"改为"缓存待删对象 + 撤销时回插原位"。
2. **归档入口降权**：从 `op-row` 文字链里挪出，改为页面底部独立的次级区域（`ghost-add` 风格描边按钮），并加二次确认 sheet 说明"归档后可在首页查看，不可恢复到进行中"。
3. **删除组合前给出影响面**：`askDelete(cid)` 弹层补一行 `当前有 N 个孔位使用该组合，删除后可继续查看历史数据，但不能再选择该条件`。

### P0-4 软键盘会挡住录入框（Android 层）

`AndroidManifest.xml` 未声明 `windowSoftInputMode`，targetSdk 35 下 WebView 默认行为不确定。s09 的「产出量」输入位于 844px 高的第 ~590px，s10 的数值行在中部偏下 —— 键盘弹出有较高概率遮挡。

```xml
<activity android:name=".MainActivity" android:windowSoftInputMode="adjustResize" ...>
```

同时 CSS 把 `min-height:100vh` 换为带回落的高度自适应，避免键盘/地址栏引起的跳动：

```css
.screen{min-height:100vh; min-height:100dvh;}
```

另：`targetSdk 35` 下 `onBackPressed()` 已被预测式返回手势取代，建议在 `AndroidManifest` 的 `<application>` 加 `android:enableOnBackInvokedCallback="true"`，并在 Java 侧迁移到 `OnBackInvokedCallback`（否则部分机型手势返回会绕过页面栈直接退出 App）。

### P0-5 数据只能导出、不能导入

`s15 数据备份` 目前是把 JSON 复制到剪贴板，却**没有任何导入入口**。用户换了手机、清了缓存、重装 APK —— 所有实验数据归零。对"记录了几十孔实验数据"的场景，这是不可接受的。

**改法（最小可用）**
- s15 备份入口改为双动作：`导出为文件`（`Blob` + `<a download>`）与 `从文件导入`（`<input type=file accept=.json>`）。
- 导入前做结构校验：`exps / archives / reagents` 必须存在，版本号不匹配时走 `normalizeDB()` 兜底而不是直接覆盖。
- 导入前强制提示「当前数据将被替换，建议先导出备份」。

---

## 三、P1 · 体验与效率，做完会有明显"产品感"

| # | 问题 | 位置 | 改法 |
|---|---|---|---|
| 1 | **跳过提示文案错误**：未分配组合的孔与已完成孔共用 `skipped` 计数，最终统一提示"所选 N 孔均已完成" | s09 `b-apply` | 拆成 `skippedDone` / `skippedEmpty` 两个计数，分别提示：`已应用 N 孔（跳过已完成 3 孔 · 未分配组合 2 孔）` |
| 2 | **s11 结果页无空态**：新实验 0 孔录入时显示 `0.0% / 0.0 / 96 孔待录入`，用户以为算错了 | s11 | 仿 s12 加空态卡：`尚未录入数据 · 返回孔板记录开始录入 →`，并隐藏分布图与组合均值区 |
| 3 | **单孔数值行看不出能编辑** | s10 | 四个 `iedit input` 加常驻下划虚线 `border-bottom:1px dashed var(--line)`，聚焦时变 `--ink2` 实线；或在行尾加淡色铅笔图标（仅未聚焦态显示） |
| 4 | **"填回收率会自动反推产出量"是隐藏强功能** | s10 | 保留功能，但在 .hint 里补一句：`直接填回收率可反推产出量；两者填其一即可` |
| 5 | **上/下一孔按 A1→H12 物理序**，跨行跳转不贴合"补录剩余孔"的真实诉求 | s10 底部栏 | 在现有双栏上叠加第三个动作：中间加一个细的「下一未录入孔 →」按钮（`seq.filter(w=>!w.done)`），无剩余时置灰 |
| 6 | **「预计占用孔数量」与实际分配可能矛盾**：s03 按"总质量÷投入量"算出 96 孔，s05 按"组合×温度×浓度"只铺 24 孔 | s03 / s06 | s03 的字段改名为「理论最大孔数（按质量估算）」，并在 s06 确认卡里并列两行：`理论最大 96 孔` / `本次实际分配 24 孔`，让差异显式可见 |
| 7 | **自动分配下孔板可点但无实质操作**，只弹一句 toast | s05 | 自动模式给板子加 `.plate.locked{pointer-events:none;opacity:.92}`，把提示前移到分段控件下方常驻小字；或点击时直接切换为手动模式并高亮当前条件 |
| 8 | **手动分配切回自动会静默丢弃手动调整** | s05 | 切回自动前若有手动改动，弹轻确认：`切回自动分配将重置为按行铺满，继续？` |
| 9 | **批量录入不支持跨行零散孔**（只能整行/整组合） | s09 | 「点选孔位」模式已支持，但缺快捷键：补「全选 / 反选 / 只选未录入 / 按行全选」四个 chip |
| 10 | **触达尺寸普遍偏小** | 全局 | 不改视觉、只扩命中区：`.nav-back{padding:5px} .combo-act{padding:12px 6px;margin:-12px -6px} .chip{padding:9px 15px} .text-act{padding:11px 2px}` —— 目标 ≥44×44 |
| 11 | **s07 实验详情缺实验设计信息**：只有 3 个统计，看不到投入量、药品、条件清单 | s07 | 在 `stat-row` 下补一行紧凑摘要：`投入 100.0 mg/孔 · 3 组合 × 2 温度 · 72 孔已分配` |
| 12 | **s13 排名行无点击暗示**，chips 折行后"组合E"孤行 | s13 | 行尾补 `›`；chips 容器改 `flex-wrap` + `justify-content:flex-start` 并把「全部组合」与各组合等宽处理，或改为横向滚动条 |

---

## 四、P2 · 打磨项（可排后续版本）

1. **字号下限**：`.pcol` 8px（72 处）、`.plabel` 9px（48 处）、`.en/.sec-en` 9px（35 处）、`.wnav-k` 9px、`.assay-sub` 9.5px。孔板行列标签受格子尺寸限制可保留，但**其余正文级小字统一提到 12px 下限**。
2. **空态文案统一**：s02/s16 是"暂无…·点击下方「新建实验」开始"，s12 是长句说明，s05/s11 无空态。抽一个 `emptyState(icon, title, action)` 组件。
3. **Toast 单槽位 + 固定 1.9s**：连续批量录入时上一条会被覆盖。加队列（最多排队 2 条）或按信息长度动态时长（`Math.max(1900, msg.length*90)`）。
4. **`tag.run` 语义过载**：既标"进行中"又标"当前最佳"。建议拆为 `.tag.run`（绿灰）与 `.tag.best`（暖棕描边）。
5. **颜色体系外泄**：归档删除用了 `#A2554A`、超额提示用了 `#A2554A`，不在 7 色令牌内。建议补 `--danger:#9A5147` 并统一（同样需满足 4.5:1）。
6. **s02 轮播卡片名被截断**（"重结晶…"）：卡片可视宽度为容器 76%，长实验名一律省略。可在卡片布局里把 `exp-name` 换成两行 clamp（`-webkit-line-clamp:2`），或把卡片宽度提到 82%。
7. **组合色与温度维度共用色板**：`组合A #C1BEB5` 与 `.well.empty` 虚线灰度接近，视觉上"已分配浅灰"与"未分配虚线"边界模糊。建议把组合 A 的色值往暖侧偏移一档（如 `#CFC2AE`）。
8. **s04 三段(组合/温度/浓度)无完成度指示**：`combo-count` 只显示"已选 N"。可在「下一步」左侧补 `已选 3 组合 · 2 温度 · 2 浓度 = 12 行` 的实时摘要，把校验前移，减少"点下一步才被 toast 拦"的挫败。
9. **无暗色模式**：`Theme.Material.Light.NoActionBar` 硬锁浅色。若考虑实验室暗光环境（样品避光、夜间记录），可加 `prefers-color-scheme` 分支，深绿主色天然适合做暗色底。

---

## 五、成熟度差距一览

| 成熟产品的隐性要求 | PureLab 现状 | 补齐动作 |
|---|---|---|
| 无障碍达标（WCAG AA） | 对比度 2.36:1、触达 17×22 | P0-1 + P1-10 |
| 危险操作可撤销 | 仅确认弹层 | P0-3 |
| 数据可迁移/可恢复 | 只出不进 | P0-5 |
| 三态完备（空/加载/错误） | 空态 2/6 页覆盖 | P1-2 + P2-2 |
| 前后端一致的状态真相 | s03 估算 vs s05 实分配两套口径 | P1-6 |
| 输入法/系统级交互适配 | 未处理软键盘与预测式返回 | P0-4 |
| 性能预算 | 单页 487 行 CSS + 1839 行 JS，DOM 全量重建（`innerHTML`），96 孔每次 `renderPlate` 重建 110 个节点 | 孔板改增量更新 `className`，避免整板重绘 |
| 数据体量边界 | 图片以 base64 存 localStorage，4 张/孔 × 96 孔理论可达数十 MB，超限只 toast「存储空间不足」 | 图片改存文件路径/IndexedDB；`save()` 加配额预检 |

---

## 六、建议执行顺序

1. **第一批（半天，收益最大）**：P0-1 颜色替换 → 重跑度量脚本清空 `lowContrast`；P1-1 文案修正；P1-2 s11 空态；P0-3-1 撤销 toast。
2. **第二批（一天）**：P0-2 孔板条件条 + 图例行号；P0-3-2 归档降权；P1-5 下一未录入孔。
3. **第三批（一天）**：P0-4 键盘/返回手势 + P0-5 导入导出；P1-3/4/6/10。
4. **收尾**：P2 打磨，重跑 harness2–40 全量回归。

---

## 七、验收方式

改动完成后，用本次同款脚本复验（脚本已落在 `.workbuddy/_ui_review/capture.js`）：

```bash
node .workbuddy/_ui_review/capture.js && python .workbuddy/_ui_review/analyze.py
```

通过标准：

| 指标 | 当前 | 目标 |
|---|---|---|
| `lowContrast` 条目 | 35+ 类 | **0** |
| `smallTap`（排除 96 孔板 `.well`） | 14 类 | ≤ 3 类（仅孔板内） |
| `tinyText`（8–11px） | 15 类 | 仅保留孔板行列标签 |
| `pageerrors` | 0 | 0 |
| 横向溢出屏数 | 0（390/320 双档） | 0 |

---

*评审人：界面交互设计专家 · 结论基于实机渲染与自动度量，非印象式判断*


---

## 八、修复落实（v2.8.0 · 2026-09-13）

按上述方案完成第一批 + 第二批改造，源码改 `app/assets/www/{app.css,app.js,index.html}`、`app/src/com/purelab/app/MainActivity.java`、`app/AndroidManifest.xml`，重打签名 APK。

### 8.1 逐项落实

| 原问题 | 修复做法 | 验证 |
|---|---|---|
| P0-1 对比度 2.36–2.39:1 | 新增 `--ink3t:#666E61`（4.77:1）、`--warm-ink:#7E5F36`（5.29:1）、`--danger:#9A5147`（5.19:1）三个**文本安全级派生色**，全量替换文本用色；原 7 色令牌原样保留，继续承担图形/描边/深绿底文字 | 度量 `lowContrast` 35+ 类 → **0 条** |
| P0-2 温度/浓度在板上不可见 | 行标签列 20→34px 承载「组合·温度」与「浓度」两行；未分配行维持字母。**未采用新增右侧列**（会把单孔从 20.8px 压到 17px） | V2 PASS；实测孔位宽仍为 21px，未缩水 |
| P0-3 危险操作无退路 | `toast(msg, undoLabel, undoFn)` 支持撤销，停留 4.2s；删除实验/归档/组合均记快照并原位恢复；归档入口移出浏览区、独立分区 + danger 色 + 不可逆说明 | V5 PASS（删除 → 撤销 → 数量复原） |
| P0-4 键盘遮挡 / 返回手势 | Manifest 加 `android:enableOnBackInvokedCallback="true"` 与 `android:windowSoftInputMode="adjustResize"`；`handleBack()` 迁至 `OnBackInvokedCallback`（API<33 走原路径）；CSS 补 `100dvh` | javac 通过；V8 PASS |
| P0-5 数据只出不进 | s15 改为「数据备份与恢复」：保存为文件 / 复制到剪贴板 / 从文件导入；`askImport(obj)` 导入前校验 JSON 结构并展示将导入的规模 | V7 PASS |
| P1 s09 跳过文案错误 | 拆成 `skipDone` / `skipUnassigned` 两个计数，分别提示，不再把未分配孔说成「已完成」 | V6 PASS |
| P1 s11 无空态 | 新增统一 `.empty-state` 组件；0 孔时给出说明与「去孔板记录录入 →」 | V4 PASS |
| P1 s10 无可编辑暗示 | 四个数值行加常驻点划下划线（聚焦转实线）；hint 补「也可直接填回收率，系统会反推产出量」 | V9 PASS |
| P1 s03/s06 口径冲突 | s03 字段改名「理论最大孔数（按质量估算）」并补说明；s06 并列「理论最大孔数」与「本次实际分配」两行 | 截图核对 |
| P1 触达尺寸普遍偏小 | 用 `::after` 扩展命中区，**视觉几何零改动**；覆盖 13 类控件 | 度量 19 类 → 9 类 |
| P1 孔板单孔 20px 点不准 | s08 新增「放大孔位」开关：孔位 44px + 横向滚动，默认视图不变，s09 点选孔位共享该状态 | V11 PASS |
| P1 s05 切回自动会静默丢改动 | 切回前做差异比对，不一致则弹确认 | V3b PASS |
| P1 s07 缺实验设计信息 | 补一行「投入 100.0 mg/孔 · 5 组合 · 96/96 孔已分配」 | V10 PASS |
| P1 s13 排名行无点击暗示 | 行尾补 `›` | 截图 |
| P2 危险色出体系 | 统一为 `var(--danger)`，含 JS 内联色 | 代码审查 |

### 8.2 验证结果

| 项 | 结果 |
|---|---|
| 新增功能回归 `verify38.js` | **12 / 12 PASS** |
| 全屏截图渲染 `capture.js` | 27 张，**0 个 JS 运行时错误** |
| 页面错误 pageerror | 0 |
| `analyze.py` 低对比度条目 | **0**（原 35+ 类） |
| `analyze.py` 命中区 <44px | 9 类（原 19 类） |
| APK 资产校验 | 13/14 命中（1 项为校验字符串写法偏差，已复核代码正确） |

### 8.3 已知取舍与未做项

1. **96 孔板单孔 20px 命中区**属物理约束（8×12 板 + 5.5 寸屏）。已提供「放大孔位」（44px + 横向滚动）+ 单孔详情的上/下一孔导航作为缓解，未强行改板面尺寸。
2. **`.combo-act`（详情/删除）命中区为 34×45**：两枚相邻文字操作的水平间距只有 12px，继续外扩会让「详情」与「删除」的命中区重叠，反而增加误删风险。这是排版约束下的最优解。
3. **预测式返回**已按规范迁移，仍需真机（Android 13+）验证手势返回是否正常回到页面栈。
4. 未做：暗色模式、Toast 队列、孔板增量重绘（当前每次 `renderPlate` 重建 110 个节点）、图片改存 IndexedDB。均为 P2，不影响本轮可用性达标。
5. **本次未重新跑 harness2–40 历史回归**：新用例覆盖了改动点，但旧的 40 套脚本建议在下一轮全量复跑一次。

### 8.4 产物

- `PureLab-v2.8.0-release.apk`（115,689 B · versionCode 40 · 已签名）
- 改动源码：`app/assets/www/app.css`、`app/assets/www/app.js`、`app/assets/www/index.html`、`app/src/com/purelab/app/MainActivity.java`、`app/AndroidManifest.xml`
- 验证脚本与截图：`verify38.js`、`capture.js`（27 张）、`analyze.py`

---

## 九、手机壳预览（新增开发工具 · 2026-09-13）

为了不再「改一版 → 重打包 → 装到手机 → 看一眼」，新增一个纯 HTML 的手机壳预览页：把 App 装进仿真的手机屏幕里，在浏览器中点着看。

### 9.1 怎么打开

在 WorkBuddy 里直接预览 `PureLab\preview.html` —— 侧边栏预览面板会打开它，屏幕里可以直接点着用。**不需要起服务，也不要开浏览器。**

`preview.html` 是**自包含单文件**：App 的 `index.html` + `app.css` + `app.js` 已被内联、base64 编码后写进本页，用 `<iframe srcdoc>` 装载。`srcdoc` 的文档天然与父页同源，所以跳屏、安全区注入、触达框都能用 —— `file://` / `http://` / 侧边栏预览三种环境行为一致。

> **不要改成 `<iframe src="另一个文件">`**：那样在 `file://` 和侧边栏预览下必然跨源，`contentWindow` 被拦，整个工具退化成一张不能交互的死图。（项目里 2026-09-04 的旧版 `preview_phone.html` 正是这个写法，因此失效 —— 这一版是它的替代。）

### 9.2 能做什么

| 能力 | 说明 |
|---|---|
| 8 种机型 | Android 360×800 / 412×915 / 432×960、iPhone 393×852 / 390×844 / 375×667、小屏 320×640、平板 768×1024；可自定义宽高 |
| 自适应缩放 | 按窗口高度自动缩放（CSS `zoom`，不留滚动空白）；也支持 100% |
| 15 屏直跳 | 顶部下拉选任意界面；在屏幕里自己点着走，选择器会反向跟随 |
| **状态栏 / 手势条仿真** | 勾选后向 iframe 注入 `--safe-t` / `--safe-b`，等效真机的 `env(safe-area-inset-*)` |
| 软键盘仿真 | 视口像 `adjustResize` 那样缩短，用来验证输入框会不会被键盘挡住 |
| 触达框覆盖层 | 绿 ≥44px / 红不足 44px，悬停显示「可视尺寸 / 命中区尺寸」 |
| 重置数据 | 清 localStorage 回到演示状态；也可只重载（保留数据） |

**安全区仿真是这个工具最值钱的部分**：App 的 `targetSdkVersion=35`，在 Android 15+ 会被**强制 edge-to-edge**，系统状态栏叠在内容之上。浏览器里预览看不到这一层，外壳把状态栏画出来才能暴露「标题被时钟压住」这类只有真机上才看得见的问题。

### 9.3 自检结果

`verify_preview.js` 在 **`file://` 与 `http://` 两种环境各跑 16 项，共 32 / 32 PASS**，0 脚本错误。关键实测值：

| 检查 | 实测 |
|---|---|
| 内联装载 | `iframe.srcdoc` 长度 145,444 字符、含 `<style>`/`<script>`、`src` 为空 |
| 跨源告警条 | 未出现 |
| 开状态栏 → 注入变量 | `--safe-t:28px`、`--safe-b:24px` |
| 顶部导航是否真的让开 | App 内 `.nav` 的 top = **28px** |
| 关状态栏 | `--safe-t:0px`、`.nav` top = **0px** |
| 15 屏逐屏直跳 | **15 / 15**，无异常 |
| 下拉跳屏 / 屏内反查 | `s08` ✓ / 选择器跟随到 `s09` ✓ |
| 触达框（s08） | 可点 5 个 · **不足 44px 0 个** · 孔位 96 个（豁免） |
| 软键盘压缩视口 | 768 → 506 px |
| 机型切换 | 320×640 时 iframe 内部视口 = 320×640 |

### 9.6 维护

- `preview_shell.html` 是外壳模板（含 `__APP_B64__` / `__SRC_INFO__` 占位符），`build_preview.py` 是构建脚本；产物 `preview.html` 里存的是**生成时的源码快照**。
- 改完 `app/assets/www/` 后重跑 `python build_preview.py`。面板里的「源码快照 mm-dd hh:mm · hash」就是用来判断预览页是否已过期的。
- 构建脚本会**强制断言占位符恰好出现 1 次**：曾因外壳的守卫代码里也写了占位符字面量，`replace` 命中两遍，产物从 241 KB 膨胀到 452 KB。守卫改用长度判断（`b64.length < 100`）后正常。
- 内联脚本时要把 `</script>` 转义成 `<\/script>`，否则会提前闭合外层脚本。

---

## 十、排版修订（v2.9.0 · 2026-09-13）

### 10.1 问题

「理论最大孔数」一行取值 `96 / 96 · 约 8 行（按质量估算）` 在手机上被从括号**中间**断开：第一行停在 `…（按质`，第二行只剩 `量估算）`，右对齐后参差不齐，观感很差。

**根因**：中文允许在任意汉字间断行，浏览器贪心填满第一行时就会在括号内部断开。CSS 的避头尾规则只保证「（」不落在行尾、「）」不落在行首，**并不阻止括号内部断行**。

### 10.2 做法

新增 `.nb{white-space:nowrap}`，把**短注记括号整体**包住：空间不足时整组换到下一行，而不是从中间断开。

| 位置 | 处理 |
|---|---|
| s03 取值行（含「超出容量」态） | `（按质量估算）`、`（板上限 96）` 包 `.nb` |
| s06 确认卡 | 原名 `（按总质量 ÷ 每孔投入量估算）` 共 15 字，nowrap 会溢出 → **缩短为 `（按质量估算）`** 后包 `.nb`，与 s03 口径统一 |
| s05 / s08 图例 | `（12 孔）`、`（复筛）` 包 `.nb` |
| 组合编辑 · 配比预览 | `（填写后自动生成）` 包 `.nb` |
| s11 最佳回收率 | `（组合 · 温度 · 浓度）` 包 `.nb` |

**为什么不用 `<br>` 硬换行**：硬换行会让平板等宽屏也无谓变成两行。`.nb` 是自适应的 —— 窄屏两行、宽屏一行（已实测，见 10.3）。

**规则边界**：`.nb` 只用于**短注记（≲8 字）**；长括号内容应当改写文案缩短，而不是硬撑一行。CSS 里已写明这条约束。

### 10.3 验证

新增 `verify_nb.js` —— **17 / 17 PASS**：

| 检查 | 实测 |
|---|---|
| 360 屏 · 括号组内部未断行 | `height / lineHeight = 0.92`（单行） |
| 360 屏 · **确实整组换到下一行** | 值文本 top=337 → 括号 top=**358** |
| 320 屏 · 同上 | top=333 → **353** |
| 320 屏 · 取值列未溢出 | `overflow = 0px`（列宽仅 126px） |
| 超出容量态 | `超出容量 · 需 200 孔（板上限 96）` 单行、不溢出 |
| s06 确认卡 | `96 孔（按质量估算）` 单行、不溢出 |
| s05 图例 ×4 | 全部单行、图例未溢出 |
| s11 hero | `（组合C）` 单行、hero 未溢出 |
| **平板 768** | 值文本与括号 top **同为 335** → 回到同一行，证明是自适应而非硬换行 |
| v38 回归 | **12 / 12 PASS**（未碰坏旧成果） |

### 10.4 产物

- `PureLab-v2.9.0-release.apk`（115,689 B · versionCode 41 · 已签名）
- 改动源码：`app/assets/www/{app.css,app.js,index.html}`、`app/version.properties`
- 新增构建脚本 `purelab-apk/build_apk.py`（见 10.5）
- 验证脚本：`verify_nb.js`；截图：`shots_preview/nb_normal_360.png`、`nb_s03_360.png`

### 10.5 构建方式变更（重要）

原先的 `build_local.sh` 已**无法执行**：本机 `bash` 会被安全策略解析到 WSL 的 `wsl.exe`（在程序黑名单里，不可批准也不可绕过），脚本里的 `grep/cut/tr/cygpath` 也都不可用；从 shell 调 `cmd.exe` 同样被拦。

改为 `purelab-apk/build_apk.py`：**纯 Python `subprocess` 直调可执行文件**，完全不经过 shell。
- `javac.exe` / `java -cp build-tools\lib\d8.jar com.android.tools.r8.D8` / `aapt2.exe` / `zipalign.exe`
- `java -jar build-tools\lib\apksigner.jar sign|verify` —— **能用 jar 就别用 .bat**

versionCode 仍从 `app/version.properties` 读取、构建后自增；本次产物 versionCode 41，下次 42。

### 9.4 附带验证价值

触达框覆盖层会用**有效命中区**判定（元素盒 + `::after` 的负向 inset 外扩量）。实测 `.text-act` 为「可视 52×22 · **命中区 66×44** ✓」——这同时也是第 8 节触达修复的**实时验证器**：一旦有人改坏了 `::after` 外扩，覆盖层会立刻变红。

### 9.5 发现的问题（非本轮回归范围）

- **320px 屏下孔板条件标签降级**：`@media(max-width:360px)` 会把两行的「组合·温度 / 浓度」退回纯字母 A–H，条件信息在板上不可见。属已知降级（320px 下 12 列孔已到极限），用户真机宽度 360+ 不受影响。若将来要支持 320，建议改为点击行标签弹出条件卡。
- 机型预设里的「平板 768×1024」主要用于检查 `#app{max-width:460px}` 的居中上限；应用锁定竖屏，所以不做横屏预览。

## 十一、孔板横屏 + 全应用排版治理（v2.10.0 · 2026-09-13）

### 11.1 孔板「横屏」视图（新功能）

**需求**：在「放大孔位」旁边加一个横屏功能，把 96 孔横过来看，空间利用率更高，从而不再依赖放大。

**为什么转 90° 就能变大**：竖屏下宽度才是瓶颈——12 列挤进约 320px，单孔只剩 20.8px，这正是「放大孔位」（44px + 横向滚动）存在的原因。把板子转 90° 后，1–12 走竖向长轴、A–H 走横向短轴，瓶颈由 12 列变成 8 列，单孔升到约 33–35px，**整板一次看全且不需要任何滚动**。

| 屏幕 | 竖屏单孔 | 横屏单孔 | 横向溢出 | 板高 / 视口 |
|---|---|---|---|---|
| 390×844 | 20.0px | **34.8px** | 0px | 486 / 844（一次看全） |
| 360×800 | 17.8px | **31.0px** | 0px | 438 / 800（一次看全） |
| 320×640 | 15.7px | **26.0px** | 0px | — |

**实现**：`renderPlate(el, mode, opts)` 新增 `opts.land`，转置建格（13 行 × 9 列：角标 + A–H 表头，每行是列号 + 8 孔）；竖屏表头（角标 + 1–12）只在非横屏时生成——第一版漏了这步，13 列表头把网格顶错位，已被自检抓住。行条件标签照旧挂到字母轴（`A·60℃ / 0.8`），坐标语义不变。

**与「放大孔位」互斥**：同开等于「既要 44px 又只放 8 列」，必然又要滚动，等于白转。两键互相关闭；状态 `plateLand / plateZoom` 全局共享给 s09 点选孔位。

**验证 `verify_land.js` —— 36 / 36 PASS**（360×800 与 390×844 各 18 项）：转置几何（A1/H1 同行、A1/A12 同列，与竖屏正好相反）、单孔变大、无横向滚动、96 孔 + 12 列号 + 8 行条件、按钮文案切换、互斥、s09 共享、无脚本错误。

### 11.2 全应用排版治理

v2.9 只处理了用户指出的那一处括号。本轮写了**字符级扫描器** `scan_typo.js`：对每屏每个可见文本节点用 `Range.getBoundingClientRect()` 逐字测量、按行高归行，检出六类缺陷——括号跨行 / 行尾悬挂分隔符 / 末行孤字 / 首行过短 / 数字单位与英文单词被拆 / 文本撑出容器。

> 扫描器自己也有两个坑，记录在此：① 它跑在外壳页，必须进 `#app` iframe 里取 `.screen.active`；② 不能用 `scrollWidth` 判溢出——项目里大量 `::after{inset:负值}` 触摸热区会被算进去，导致「删除」「批量录入」这类按钮全部误报，改用 `Range` 量文本自身宽度。

**首轮 360×800 扫出 32 处**，逐条修复后 **4 机型（320/360/390/768）× 15 屏 = 60 项全部归零**：

| 缺陷 | 根因 | 处理 |
|---|---|---|
| 首页实验名只剩 2 字（`重结…`，320 屏整行消失） | `row1` 塞了编号+名称+标签+删除四样，`flex:1` 的名称被压到 51px | **名称独占一行**：第一行 编号/标签/删除（`exp-del` 靠右），第二行完整名称 `.exp-title` |
| 「96孔板」被拆成「96孔 / 板」 | `.exp-meta` 三个 span 被 flex 压到比内容还窄 | `.exp-meta` 改 `flex-wrap:wrap` + `span` 整体 `nowrap`；「当前最佳」缩短为「最佳」 |
| 归档副标题「…已 / 完成」 | 「· 已完成」与区块标题「已归档实验」重复且挤 | 渲染时 `archSub()` 去掉「· 已完成」（旧数据兼容）；窄屏缩图标与间距 |
| 「结果」芯片竖着断成「结 / 果」 | `.nav-side` 56px 装不下 图标13+间距5+文字24 | `.nav-side` 放宽到 64px + `.nav-chip` `nowrap` |
| 「回收率85– / 90」比区间悬尾 | `.dist-k` 固定 64px 装不下 71px | 放宽到 76px + `nowrap` |
| 「…C7· / 组合C」分隔符悬尾 | 窄统计列在 `·` 后断行 | `C7 · 组合C` 用 `.nb` 绑成一块，整块换行 |
| 排行行「组合 / C」（320 屏 20 处） | 五列挤爆，`flex:1` 的组合列只剩 32px | `.rank-combo` `nowrap`；≤340px 隐藏纯度列（详情页仍有）+ 收紧间距 |
| 配方比式「乙醇:丙酮: / 水=2:1: / 1」 | 比式是原子信息，断开即不可读 | `recipeHTML()` 把 `= 比值` 用 `.nb` 绑块，窄屏整块落到第二行 |
| 提示语末行只剩「承」「果。」 | 长句贪心断行 | `body{text-wrap:pretty}` 全局兜底 + 关键词组 `.nb`（「自动继承」「（默认不选，可多选）」） |
| 标语「…不做自动 / 推荐」 | 320px 标语行超宽 20px | 文案精简为「数据本地留存 · 不做自动推荐」 |

**验证 `scan_typo.js`**：`node scan_typo.js`（全机型）或 `node scan_typo.js 0 6`（指定机型）。报告写 `typo_report.json`，控制台按屏列出「屏 / 选择器 / 原文 / 缺陷类型」。

### 11.3 回归与产物

- `verify38.js` **12/12**、`verify_nb.js` **17/17**、`verify_land.js` **36/36**、`scan_typo.js` **60 项 0 缺陷**
- QA harness 全量（harness.js–harness40）复跑：以 **v2.9.0 APK 解包内容作基线对照**，逐条比对失败清单——
  除 `harness29 C3`（本轮把 s15 的「C4 · 组合C」绑进 `.nb`，分隔符文本变化）为**本轮引入**并已修正（正则放宽，复跑 6/6）外，
  **其余失败与基线完全一致**，均为历史遗留的陈旧用例：引用已删除的 `s01` 启动页（harness 1/2/4/6/8/14/16）、
  s05 重做前的旧交互（harness 17/22）、v2.9 加注记括号前的逐字文案（harness 25）。这些用例需要在下一轮 QA 里统一翻新。
- 改动源码：`app/assets/www/{app.css,app.js,index.html}`、`app/version.properties`（2.10.0 · versionCode 42）
- 新增脚本：`verify_land.js`、`scan_typo.js`、`probe.js`（盒模型探针）、`shot_final.js` / `shot_320.js`
- 截图：`shots_final/01_s02_首页_修复后.png`、`04_s08_横屏.png`、`05_s09_点选孔位_横屏.png`、`06_s08_320_横屏.png`
- 预览页已重建：`preview.html`（自包含，WorkBuddy 侧边栏直接打开）
