# 2026-09-04 PureLab V1 黑盒测试战役（步骤1~9）

## 执行方式
- 用 playwright-core + agent-browser 的 Chrome 写了 7 个 harness 脚本（purelab-apk/qa/harness*.js），纯黑盒：只按界面文本点击/填值，记录屏幕实际反馈。
- 踩坑：agent-browser daemon 多标签绑定漂移（eval 落到 about:blank），eval 触发导航后上下文丢绑；解决=单标签+getClientRects 可见性判定+每脚本独立会话。
- 应用由 `python -m http.server 8899`（purelab-apk/app/assets/www）提供。

## 核心结论（P0）
1. 创建实验阻断：s02[新建实验→] 弹空壳 BottomSheet（无标题无内容无输入框），s03 表单存在但入口断。
2. 写入链路全断：s10[编辑] 无反应、s08[编辑数据] 死按钮、s08[批量] 空壳 sheet 且点遮罩关不掉 → 全应用无法修改任何数据。
3. s09 批量录入是唯一可用表单，但[应用到所选孔位]提交无效：默认/覆盖模式下孔板统计均不变（63 plain/32 empty/1 sel best 恒定），无 toast。
4. s14 试剂库、s15 我的、s11 结果总览、s13 排名无入口不可达；s12[查看完整排名]死按钮。
5. 异常输入（0/负/超大/文字/单位）全部静默吞掉，无校验无提示；toast 系统全程未触发过。

## 通过项
- 浏览链路：s01→s02→s07→s08→s09/s10/s12 全通，‹ 返回链正常，reload 后演示数据一致恢复。
- 静态一致性：94.2%/C7/组合C丙酮:水3:1 在 s02/s07/s10/s12 四处一致；C7 详情计算 94.2/100 正确。
- 全程 0 JS 崩溃；s12"收藏后…不做任何自动推荐"文案符合无 AI 推荐禁令。
- 交互元素统计：s03(7)/s04(3)/s05(4)/s06(2) 内容完整但不可达（上游断）。

## 视觉（步骤8）
- 实现走"提示词规范"（无 Tab/文字CTA+细线/纸面卡），参考图是"底部Tab+胶囊+深绿主卡+缩略图"——两者结构性冲突，实现未贯彻此前"参考图优先"决策，需用户裁决。

## 产出
- qa/qa_runB~H.json（证据日志）、qa/shots/app_s*.png（可达屏截图）、完整报告 md。

## PureLab 修复战役收尾（RUN-K 全绿 + APK 重打）
- QA 报告勘误：原 6 个 P0 中 5 个为 harness clickText 误点 wrapper 的误报；真实缺陷 7 项全部修复。
- 新发现真实缺陷：s15「我的」页无返回按钮（页面内无法退回 s02），已补 nav-back。
- 设计确认（非 bug）：s03 下一步不做空名校验（校验门在 s06 创建实验处）；s09 产出量 0 为合法数据；s07 mini 孔板点击→s08、s08 全板点击→s10。
- RUN-K（harness11.js）24/24 检查点通过：新实验真实创建（EXP-04 空板）、批量校验三连、仅填充未完成跳过+覆盖应用、B3 逐孔编辑 95%、清空清除、best=B3@90% 动态、s12 收藏→s13 排名第一、reload 持久化。唯一 pageError 为 favicon 404。
- harness 修 bug 经验：dbState 须对 localStorage null 加守卫；拦截回 s03 后须重走向导；toast 1900ms 残留会污染断言。
- APK 已重打签名：purelab-apk/PureLab-v1.0-debug.apk（50KB），包内资产已验证含 s15 返回按钮与 bestCoord。

## 步骤8~12 验收战役（2026-09-04 下午）
- 步骤8 视觉还原：15屏截图对比，整体还原度约70%；最大偏差=向导缺stepper、深绿主卡变白、s06缺预览、图像资产缺失；CTA/Tab 属约束性偏差待用户裁决。
- 步骤9 交互探测：126项+代码审计，无真死按钮；3处假按钮观感（s02归档卡/s04组合卡/s14试剂卡无绑定）。
- 步骤10 压力：11/11；真实发现 btn-create 无防重复提交（三连击 EXP-04→05→06 跳号）、w-edit 隐藏态越屏触发（已加防御守卫并重打 APK）。
- 步骤11 真实场景：18/18 走通完整闭环（62孔、35次点击、最佳D7@96.1%）；创建后返回栈残留向导 s06 属 UX 风险。
- 步骤12 五维评分：功能88/交互85/适配86/闭环95/视觉70；【必须立即修改】=创建防重复提交+创建后清向导栈。
- 报告：purelab-apk/PureLab_V1_最终验收报告_步骤8-12.md；脚本 harness11~15.js + shots_vis/。
- harness 经验：clickVisibleText 必须用 startsWith（按钮带→后缀）；chip 激活类是 .on；apply 成功会自动 back()，脚本勿多退；getElementById 可点中隐藏元素引发假阳性。

## 方向性修复落实（2026-09-04 16:57）
- P0×2：btn-create 加 createLock（renderConfirm 重置）防重复提交；创建后 stack=['s01','s02','s07'] 清向导栈。
- 视觉还原：s03~s06 补 ①②③ stepper；s02 EXP 卡与 s11 hero 改深绿渐变白字；s06 补 96 孔预览+图例；s01 补 SVG 植物插画；s02 归档卡补叶片缩略图。
- 交互补全：s07 迷你板直达 s10（少一跳）；s04 新建组合弹配方表单（F/G/H 最多3个）+组合卡详情/自定义可删；s14 试剂卡详情 sheet；s15 补帮助与反馈。
- 有意保留：CTA 文字线/无 Tab（硬约束待裁决）；s12/s14 第二 segment（内容未定义不做假 UI）。
- 回归：harness16 修复验证 18/18 PASS + harness14 压力 11/11 PASS，APK 重打 54KB（16:57）。
- 新增 DB 字段：customCombos[]（旧数据自动迁移 if(!DB.customCombos)）。
